import React from "react";
import { render } from "@testing-library/react";
import Profile from "./Profile";
// it("Profile renders text prop", () => {
//     const { getByText } = render(
//     <Profile />
//     );
//     expect(getByText("Hello World from test")).toBeTruthy();
// });
// it("Profile renders with no prop value provided", () => {
//     const { getByText } = render(<Profile text={""} />);
//     expect(getByText("no prop value provided")).toBeTruthy();
// });
